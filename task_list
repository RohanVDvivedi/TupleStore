 * move to slotted page model permanently (use the fixed tuple length model only when the tuple is fixed sized)

 * in a separate repo build Btree index, hash index and SST persistent datastructures using SimpleTupleStorageModel and BufferPool libraries