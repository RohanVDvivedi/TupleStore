
 * insert_sorted_tuples_from_page(void* page, const void* page_src, uint32_t start_index, uint32_t end_index, const tuple_def* def);

 * provide a tuple_as_struct interface to bridge the gap between Cutlery data structures and the tuple from SimpleTupleStorageModel

 * dynamic programming approach to calculate offsets in a varibale sized tuple